{
  "routes": [
    {
      "method": "GET",
      "path": "/observations",
      "handler": "observation.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/observations/count",
      "handler": "observation.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/observations/:id",
      "handler": "observation.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/observations",
      "handler": "observation.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/observations/:id",
      "handler": "observation.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/observations/:id",
      "handler": "observation.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
